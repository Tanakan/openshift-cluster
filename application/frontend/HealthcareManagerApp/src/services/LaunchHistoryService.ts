import { LaunchHistory } from "../models/LaunchHistory";
import { useAxios } from "./AxiosInstance";
 

export default class LaunchHistoryService {

    // public async listLaunchHistories(): Promise<LaunchHistory[]> {
    //     const response = await useAxios().get('launch_history');
    //     return response.data
    // }

}